
// Array => []

    // push() => add an object to the end of array
    // pop()  => removes an object in the end of array
    // unshift() => adds an object to the beginning of array
    // shift() =>removes an object from the beginning of array
    // includes() => check that obj is in the array

var colors=["red","blue","green"]

console.log(colors.includes("green"))


// colors.unshift("pink")
// console.log(colors)

// colors.shift()
// console.log(colors);


// colors.pop()
// console.log(colors)

// colors.push("yellow")
// console.log(colors)

